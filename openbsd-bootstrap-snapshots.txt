# Bootstrap OpenBSD snapshot for later Ansible provisiong

#instal vanilla OpenBSD snapshot from .iso
#add a user
#ssh-copy-id -i ~/.ssh/id_rsa <user>@<ip>
#echo "permit nopass keepenv <user> as root" > /etc/doas.conf
#export PKG_PATH=http://ftp3.usa.openbsd.org/pub/OpenBSD/snapshots/packages/amd64
#pkg_add -i git python
#ln -sf /usr/local/bin/python2.7 /usr/local/bin/python


